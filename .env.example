# Neon database connection string.
# Use the pooled connection string in Vercel for better serverless behavior.
DATABASE_URL=

# Optional: keep a direct URL for migrations or admin tools.
DATABASE_URL_DIRECT=
